#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - characters upper and lower case
 *
 * Return: zero
 */

int main(void)
{
char alfabeto, e, q;
e = 'e';
q = 'q';
for (alfabeto = 'a'; alfabeto, <= 'z'; alfabeto++)
{
if (alfabeto, != e && alfabeto, != q)
putchar(alfabeto);
}
putchar('\n');
return (0);
}
